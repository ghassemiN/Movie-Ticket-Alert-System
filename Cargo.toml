[package]
name = "book"
version = "0.1.0"
edition = "2021"

[dependencies]
dotenv = "0.15.0"
reqwest = { version = "0.10.0-alpha.2", features = ["blocking"] }
scraper = "0.19.0"
tokio-util = { version = "0.6.10", features = ["codec"] }